version: '3.8'

# Development environment - Uses external database
# Server: 14.225.212.145:1433
# Database: AciPlatform
# User: dev_user1
# Password: Dev@123456
#
# Note: Local database container removed - connects to external database

services: {}

networks:
  aciplatform-dev:
    driver: bridge

volumes:
  db-data-dev:
